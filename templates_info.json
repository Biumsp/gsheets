{
    "dropdown": {
        "parameters": {
            "$sheet_id": "Single sheet ID (int)",
            "$start_row": "Range starting row (included, 0-based) (string)",
            "$end_row": "Range ending row (excluded, 0-based) (string)",
            "$start_col": "Range starting col (included, 0-based) (string)",
            "$end_col": "Range ending col (excluded, 0-based) (string)",
            "$values_list": "Dropdown list values (List[string])"
        }
    }
}